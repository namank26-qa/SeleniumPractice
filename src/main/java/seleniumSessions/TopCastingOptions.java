package seleniumSessions;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.remote.RemoteWebDriver;

public class TopCastingOptions {

	public static void main(String[] args) {
		
		//WebDriver driver = new ChromeDriver(); //valid and recommended
		
		//WebDriver driver = new WebDriver(); //not valid because WebDriver is an interface.
		
		//WebDriver driver2 = new RemoteWebDriver(remoteAddress, capabilities); //valid and recommended only for Remote server execution.

	}

}
